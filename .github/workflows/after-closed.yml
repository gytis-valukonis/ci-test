on:
  pull_request: 
    types:
      - closed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Job
        run: |
          echo "deployed"
